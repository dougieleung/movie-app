import React from 'react';
import DetailContainer from '../containers/DetailContainer';

const DetailScreen = ({ navigation, route }) =>{ return (<DetailContainer navigation={navigation} route={route}/> ) }

export default DetailScreen;